<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Odyssey.Views.QuickActionsDialog.ManageQuickActionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Odyssey.Views.QuickActionsDialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:helpers="using:Odyssey.Shared.Helpers"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid.Resources>
            <local:EnumToGlyphConverter x:Key="EnumToGlyphConverter" />
        </Grid.Resources>

        <ListView x:Name="quickActionListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <helpers:SymbolIconEx Symbol="Volume3"/>

                        <TextBlock Grid.Column="1" Text="{Binding Label}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
